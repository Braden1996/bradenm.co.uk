/**
 * The tones a tag can be painted in. Each one resolves to a `--color-tag-<tone>-*`
 * triple in `src/styles/theme.css`; adding a tone means adding it here and there.
 */
export type TagTone = "neutral" | "sand" | "clay" | "amber" | "sage" | "mist" | "steel";
