---
import ScrollFadeFrame from "../../../components/molecules/scroll-fade-frame.astro";
import type { BookViewModel } from "../lib/bookshelf-data";
import BookshelfRow from "../molecules/bookshelf-row.astro";
import BookshelfHeader from "./bookshelf-header.astro";

interface Props {
  books: BookViewModel[];
}

const { books } = Astro.props;
const eagerCoverCount = 12;
---

<ScrollFadeFrame class="list-wrap" data-list-wrap initialFade="bottom" tone="subtle">
  <div class="book-scroll" data-scroll-fade-viewport>
    <BookshelfHeader />
    <div class="book-list" data-book-grid data-book-list>
      {books.map((book, index) => <BookshelfRow book={book} eager={index < eagerCoverCount} />)}
    </div>
  </div>
</ScrollFadeFrame>

<style>
  .list-wrap {
    flex: 1;
    padding-top: 0;
  }

  .book-scroll {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow-y: auto;
    scrollbar-gutter: stable;
    min-height: 0;
    scrollbar-width: thin;
    scrollbar-color: var(--color-border-strong) transparent;
    overscroll-behavior: contain;
  }

  .book-list {
    flex: 1;
    display: grid;
    grid-template-columns: var(--bookshelf-columns);
    column-gap: var(--bookshelf-column-gap);
    align-content: start;
    min-height: 0;
    padding: 0 var(--bookshelf-inline-padding) 0.45rem;
  }

  @media (max-width: 760px) {
    .book-list {
      grid-template-columns: var(--bookshelf-columns);
      column-gap: var(--bookshelf-column-gap);
    }
  }
</style>
