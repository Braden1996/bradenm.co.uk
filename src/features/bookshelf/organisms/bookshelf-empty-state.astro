<div class="empty-state" data-empty-state hidden>
  <p>No titles match that search.</p>
  <button class="empty-reset" type="button" data-empty-reset>Clear search</button>
</div>

<style>
  .empty-state {
    padding: var(--space-24) var(--bookshelf-inline-padding);
    text-align: center;
    color: var(--bookshelf-text-muted);
    font-size: var(--text-body);
  }

  .empty-reset {
    display: inline-flex;
    align-items: center;
    height: var(--control-lg);
    margin-top: var(--space-8);
    padding: 0 var(--space-12);
    /* A third of the control it wraps, which is exactly --control-lg tall. */
    border-radius: var(--radius-md);
    border: 1px solid var(--bookshelf-stroke);
    background: var(--color-surface-subtle);
    color: var(--bookshelf-text-muted);
    font-size: var(--text-ui);
    cursor: pointer;
    transition: background-color 180ms ease;
  }

  .empty-reset:hover {
    background: var(--color-surface-interactive-hover);
  }

  @media (prefers-reduced-motion: reduce) {
    .empty-reset {
      transition: none;
    }
  }
</style>
