/**
 * The pencil rule over a career card's footer, in CSS px. The bake lives in
 * about-passage (with the page's other strokes) and the box that shows it in
 * career-card; both have to agree on its size, so the two numbers live here.
 *
 * The width is the card at its widest less both gutters. The bake is stretched
 * to whatever the card actually is, which a line this low in frequency takes
 * without complaint.
 */
export const CAREER_RULE_HEIGHT = 14;
export const CAREER_RULE_WIDTH = 456;
